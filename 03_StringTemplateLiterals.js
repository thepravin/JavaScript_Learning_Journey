/*
    => Template literal is created using the backtick (`) character.

 */

const lastFourDigit = "8882";
const balance = "705";
const desc = `My account number is ${lastFourDigit}`;
const bala = `My account number balacne is $${balance}`;
console.log(desc);
console.log(bala);
